node_modules/
dist/
coverage/
test-results/
playwright-report/
playwright/.cache/

# Full-build-tree SBOMs (~1.3 MB) are release assets only; the runtime-closure
# SBOMs next to them (sbom/bom.cdx.json, sbom/bom.spdx.json) are committed.
sbom/bom.full.*

*.log
*.tsbuildinfo
.DS_Store
.npmrc
